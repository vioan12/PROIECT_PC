#include"mersul_trenurilor.h"
#include<stdlib.h>
#include<stdio.h>
#define INFINIT 500000

void dijkstra(int start)
{/*
    int i,min,k,ok,curent;
    tata=malloc((*n_localitati+1)*sizeof(int));
    distanta=malloc((*n_localitati+1)*sizeof(int));
    harta=malloc((*n_localitati+1)*sizeof(int));
    curent=start;
    for(i=1;i<=*n_localitati;i++){
		harta[i]=INFINIT;
		tata[i]=0;
		distanta[i]=INFINIT;
	}
	harta[curent]=0;
	tata[curent]=0;
	for(i=1;i<=*n_localitati;i++)
		if(graf_distanta[curent+i]!=INFINIT && graf_distanta[curent+i]!=0)
			harta[i]=graf_distanta[curent][i];
	for(i=1;i<=n;i++)


*/
}
